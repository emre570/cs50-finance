#include <stdio.h>
#include <cs50.h>

int main()
{
    int basamak;
    int sayı = get_int("Sayı giriniz:");
    basamak = 1;
    do
    {
        sayı = sayı/10;
        basamak++;
    }
    while(sayı>=10);
    printf("Basamak sayısı= %i\n", basamak);
}
